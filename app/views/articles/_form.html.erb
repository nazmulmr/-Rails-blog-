<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2 class="alert alert-danger"><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title , {class: "form-control"} %>
  </div>
  <div class="field">
    <%= f.label :post %><br>
    <%= f.text_area :post, {class: "form-control"} %>
  </div>
  <div class="actions ">
    <%= f.submit %>
  </div>
<% end %>
